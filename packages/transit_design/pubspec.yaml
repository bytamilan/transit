name: transit_design
description: Runtime-themed design system for Transit.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  transit_core:
    path: ../transit_core

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
